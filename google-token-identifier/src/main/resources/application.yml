# Datasource configuration
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/google_token_identifier_db
    username: gti_user
    password: password
  jpa:
    hibernate.ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect 



# Server configuration
server:
  port: 8090
  
  
  